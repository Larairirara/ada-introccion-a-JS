 const prompt = require("prompt-sync")({ sigint: true }); 
let numeroA = parseFloat(prompt(`ingrese valor de tinta cyan: `));
let numeroB = parseFloat(prompt(`ingrese valor de tintas magenta: `));
let montoFinal = (numeroA + numeroB)
console.log(`el monto final a pagar es de:`, montoFinal);
 